# CipherStudio Frontend

React + Vite frontend for CipherStudio IDE.

## Setup

```bash
npm install
npm run dev
```

## Build for Production

```bash
npm run build
```

## Environment Variables

Create `.env` file:

```
VITE_API_URL=http://localhost:5000/api
```

For production, set to your deployed backend URL.
